name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Quality checks
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24.20.0"
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm format:check
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm test:e2e
      - run: pnpm test:integration
      - run: pnpm build

  # 再起動時のデータ・ID保持はqualityの実DBテストで検証する。
  compose:
    name: Compose smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      COMPOSE_PROJECT_NAME: spot-finder-ci
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Start all services with an empty database
        run: docker compose up --build -d
      - name: Check imported spots
        run: |
          count=$(docker compose exec -T db psql -U spot_finder -d spot_finder -At \
            -c "SELECT count(*) FROM spots")
          test "$count" = 200
      - name: Check nearby spot search through Web
        run: |
          curl --fail --silent --show-error \
            --retry 60 --retry-delay 2 --retry-max-time 120 \
            --retry-connrefused --retry-all-errors --max-time 5 \
            'http://127.0.0.1:3000/api/spots/nearby?latitude=35.681236&longitude=139.767125&radiusKm=0.1' \
            | jq --exit-status 'length == 1 and .[0].name == "東京駅"'
      - name: Check Web
        run: |
          status=$(curl --fail --silent --show-error \
            --retry 60 --retry-delay 2 --retry-max-time 120 \
            --retry-connrefused --retry-all-errors --max-time 15 \
            --output /dev/null --write-out '%{http_code}' \
            http://127.0.0.1:3000)
          test "$status" = 200
      - name: Show logs on failure
        if: failure()
        run: docker compose logs --no-color
      - name: Clean up
        if: always()
        run: docker compose down --volumes --remove-orphans
